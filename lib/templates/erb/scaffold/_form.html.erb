<%= turbo_frame_tag "tag_#{tag.id}" do %>
  <%= form_with(model: tag, local: false) do |form| %>
    <div class="form-group">
      <%= form.label :name, "Nom du tag", class: "form-label" %>
      <%= form.text_field :name, class: "form-control" %>
    </div>
    <div class="d-flex justify-content-between">
      <%= form.submit "Mettre à jour le tag", class: "btn btn-primary" %>
      <%= link_to 'Annuler', tags_path, class: 'btn btn-secondary' %> <!-- Tu peux personnaliser l'action "Annuler" -->
    </div>
  <% end %>
<% end %>
